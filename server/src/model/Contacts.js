class Contacts{
    constructor(sender_id, receiver_id, message, sent_datetime){
        this.sender_id = sender_id
        this.receiver_id = receiver_id
        this.message = message
        this.sent_datetime = sent_datetime
    }
}

module.exports = Contacts
